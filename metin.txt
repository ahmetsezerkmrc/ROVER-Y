{
                var component = Qt.createComponent("pages/Signin.qml")
                var win = component.createObject()
                win.show()
                visible = false

            }
        }



@Slot(str)
    def recisteremail(self, email1):
        emailpy = email1

    @Slot(str)
    def recisterpassword(self,password1):
        passwordpy = password1

    @Slot(str,str)
    def recister(self,example):
        email = recisteremail()
        password = recisterpassword()
        user = auth.create_user_with_email_and_password(email, password)


        self.setLogin.emit("NOT yours")